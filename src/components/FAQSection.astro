---
import yaml from 'js-yaml';
import fs from 'fs';
import path from 'path';

const faqDataPath = path.join(process.cwd(), 'src/data/faq.yaml');
const faqData = yaml.load(fs.readFileSync(faqDataPath, 'utf8'));
const faqs = faqData.items;
---

<section id="faq" class="section bg-sand-50">
  <div class="container">
    <h2 class="text-center mb-4">Často kladené dotazy</h2>
    <p class="text-center text-charcoal-600 mb-12 max-w-2xl mx-auto">
      Odpovědi na nejčastější dotazy našich hostů
    </p>

    <div class="max-w-7xl mx-auto">
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
        {faqs.map((faq, index) => (
          <details class="group bg-white rounded-lg shadow-sm hover:shadow-md transition-shadow border border-sand-200">
            <summary class="cursor-pointer px-6 py-4 font-semibold text-wood-900 flex items-center justify-between list-none">
              <span class="flex-1">{faq.question}</span>
              <svg
                class="w-5 h-5 text-moss-600 transform transition-transform group-open:rotate-180"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
              </svg>
            </summary>
            <div class="px-6 pb-4 text-charcoal-700">
              {faq.answer}
            </div>
          </details>
        ))}

        <!-- Still have questions? - Now inside grid -->
        <div class="lg:col-span-2 text-center bg-moss-50 border-2 border-moss-200 rounded-lg shadow-sm hover:shadow-md transition-shadow p-8 flex flex-col items-center justify-center">
          <h3 class="text-xl font-semibold text-wood-900 mb-3">Máte další otázky?</h3>
          <p class="text-charcoal-600 mb-6">Neváhejte nás kontaktovat, rádi vám pomůžeme!</p>
          <a href="#kontakt" class="btn-primary">
            Kontaktovat nás
          </a>
        </div>
      </div>
    </div>
  </div>
</section>
